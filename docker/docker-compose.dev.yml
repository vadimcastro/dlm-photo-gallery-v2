# dlm-photo-gallery-v2 development overrides
# Use with: docker compose -f ../../vadimOS/docker/docker-compose.base.yml -f docker-compose.yml -f ../../vadimOS/docker/docker-compose.dev.yml -f docker-compose.dev.yml

services:
  frontend:
    environment:
      - NEXT_PUBLIC_ADMIN_NAME=Dan
      - NEXT_PUBLIC_ADMIN_ROLE=Administrator
      - NEXT_PUBLIC_ADMIN_INITIALS=D
    command: sh -c "npm install && npm run dev"
      
  api:
    env_file:
      - /Users/vadimcastro/Desktop/PROJECTS/dlm-photo-gallery-v2/.env.development
    volumes:
      - ../backend/.env:/app/.env:ro